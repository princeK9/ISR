# Data & model artifacts
# Leading slashes anchor these to the repository root. Without them the patterns
# match at every directory level, which also excluded the src/models/ package.
/data/
/models/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Jupyter
.ipynb_checkpoints/

# Kaggle credentials (never commit these)
kaggle.json

# OS / editor
.DS_Store
.vscode/
.idea/

preparation/